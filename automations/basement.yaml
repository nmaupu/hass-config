- alias: Turn ON basement escalier light on vibration
  trigger:
    platform: state
    entity_id: sensor.0x00158d00031b1a5d_action
    to: 'vibration'
  condition:
    - condition: state
      entity_id: light.basement_escalier_switch
      state: 'off'
  action:
    - service: light.turn_on
      data:
        entity_id:
          - light.basement_escalier_switch
